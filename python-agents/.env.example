# Python ADK Agent Service Configuration

# Flask Configuration
FLASK_ENV=development
PORT=5000

# ============================================================================
# GOOGLE AI STUDIO API KEY (REQUIRED)
# ============================================================================
# How to get your Google AI Studio API Key:
# 1. Go to https://aistudio.google.com/
# 2. Sign in with your Google account
# 3. Click "Get API key" in the left sidebar
# 4. Click "Create API key" button
# 5. Choose "Create API key in new project" or select existing project
# 6. Copy the generated API key and paste it below
# 
# Note: This key is free with generous usage limits for development
GOOGLE_API_KEY=your_google_ai_studio_api_key_here

# ============================================================================
# GOOGLE CLOUD CONFIGURATION (OPTIONAL - for Vertex AI)
# ============================================================================
# Only needed if you want to use Vertex AI instead of AI Studio
# How to set up Google Cloud:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable the Vertex AI API:
#    - Go to APIs & Services > Library
#    - Search for "Vertex AI API" and enable it
# 4. Create a service account:
#    - Go to IAM & Admin > Service Accounts
#    - Click "Create Service Account"
#    - Give it a name like "adk-agent-service"
#    - Grant "Vertex AI User" role
#    - Create and download JSON key file
# 5. Set the path to your JSON key file below
GOOGLE_CLOUD_PROJECT=your_project_id_here
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json

# ADK Model Configuration
DEFAULT_MODEL=gemini-2.5-flash
# Alternative models: gemini-1.5-pro, gemini-1.0-pro

# ADK Agent Configuration
AGENT_TIMEOUT=30
MAX_ITERATIONS=10
TEMPERATURE=0.7

# Logging Configuration
LOG_LEVEL=INFO
ADK_LOG_LEVEL=INFO

# CORS Configuration (for React frontend)
CORS_ORIGINS=http://localhost:3000

# ============================================================================
# OPTIONAL: OPENAI CONFIGURATION
# ============================================================================
# How to get OpenAI API Key (if you want to use GPT models):
# 1. Go to https://platform.openai.com/
# 2. Sign up or log in to your account
# 3. Go to https://platform.openai.com/api-keys
# 4. Click "Create new secret key"
# 5. Give it a name and copy the key
# 6. Note: OpenAI is a paid service, you'll need to add billing info
# OPENAI_API_KEY=your_openai_api_key_here

# ============================================================================
# OPTIONAL: ANTHROPIC CONFIGURATION
# ============================================================================
# How to get Anthropic API Key (if you want to use Claude models):
# 1. Go to https://console.anthropic.com/
# 2. Sign up or log in to your account
# 3. Go to https://console.anthropic.com/settings/keys
# 4. Click "Create Key"
# 5. Give it a name and copy the key
# 6. Note: Anthropic is a paid service, you'll need to add billing info
# ANTHROPIC_API_KEY=your_anthropic_api_key_here